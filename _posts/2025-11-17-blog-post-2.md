---
title: 'Ollama环境配置-从0开始本地跑大模型'
date: 2025-11-17
permalink: /posts/2025/11/blog-post-2/
tags:
  - Ollama
  - LLM

---

**🖥️ 从零开始配置一台可用 Ollama 本地部署大模型 的机器**

本章将从系统初始环境开始，一步步完成 NVIDIA 驱动检查及CUDA 环境准备、Conda 配置、PyTorch GPU 版本安装，到最终运行 Ollama 大模型。

**1. 检查 NVIDIA 驱动 & CUDA**

首先在终端输入`nvidia-smi`，从图中可以看到我们有一台崭新的5090显卡，以及配备了13.0版本的cuda
![image](/images/nvidia.png)

**2. 安装Miniconda并创建独立环境**

```bash
# 安装系统包
sudo apt install wget bzip2 -y

# 下载并运行Miniconda安装脚本
wget https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh
bash Miniconda3-latest-Linux-x86_64.sh

# 激活conda查看版本
source ~/.bashrc
conda --version

# 创建独立虚拟环境 防止版本冲突
conda create -n tzy python=3.10

# 激活环境
conda activate tzy
```

**3. 安装PyTorch**

访问pytorch官网 https://pytorch.org/get-started/locally/ 根据环境选择对应版本：
![image](/images/pytorch.png)
```bash
pip3 install torch torchvision --index-url https://download.pytorch.org/whl/cu130
```
测试环境是否成功：(返回True则代表环境安装成功)
```python
import torch
torch.cuda.is_available()
```

**4. 安装Ollama**

通常可以直接使用以下命令直接下载：
```bash
curl -fsSL https://ollama.com/install.sh | sh
```
但是经常会出现网络连接问题，可以使用GitHub Release：
```bash
wget https://github.com/ollama/ollama/releases/download/v0.12.11/ollama-linux-amd64.tgz
```
或下载到本地然后scp上传，然后执行
```bash
# 解压
sudo tar -zxvf ollama.tgz -C ./
# 设置权限
sudo chmod +x ./bin/ollama
# 验证版本
./bin/ollama version
# 启动！
./bin/ollama serve
```

**5. 运行测试模型**

```bash
./bin/ollama run llama3:8b
```
现在你已经可以在本地和大模型交互啦！
![image](/images/llama.png)







